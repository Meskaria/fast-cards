# Migration `20201004090646-added-token-fields-to-user`

This migration has been generated by Adriana Olszak at 10/4/2020, 11:06:46 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" ADD COLUMN "accessToken" text   ,
ADD COLUMN "refreshToken" text   ,
ADD COLUMN "lastLogin" timestamp(3)   ,
ADD COLUMN "isEmailVerified" boolean   NOT NULL DEFAULT false
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200927185435-add-is-deleted-to-user-model..20201004090646-added-token-fields-to-user
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
   password = env("DATABASE_PASS")
   user     = "postgres"
 }
@@ -12,26 +12,30 @@
   provider = "prisma-client-js"
 }
 model Profile {
-  id     Int     @default(autoincrement()) @id
-  bio    Json?
-  user   User    @relation(fields: [userId], references: [id])
-  userId Int     @unique
+  id      Int     @default(autoincrement()) @id
+  bio     Json?
+  user    User    @relation(fields: [userId], references: [id])
+  userId  Int     @unique
 }
 model User {
-  id        Int      @default(autoincrement()) @id
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  email     String   @unique
-  password  String
-  name      String
-  surname   String
-  isDeleted Boolean   @default(false)
-  access    AccessRight
-  languages UserOnLanguageOnProficiency[]
-  profile   Profile?
+  id                Int      @default(autoincrement()) @id
+  createdAt         DateTime @default(now())
+  updatedAt         DateTime @updatedAt
+  email             String   @unique
+  password          String
+  name              String
+  surname           String
+  isDeleted         Boolean   @default(false)
+  accessToken       String?
+  refreshToken      String?
+  lastLogin         DateTime?
+  isEmailVerified   Boolean   @default(false)
+  access            AccessRight
+  languages         UserOnLanguageOnProficiency[]
+  profile           Profile?
   @@index([email])
 }
@@ -49,10 +53,10 @@
   userId          Int
 }
 model Proficiency {
-  id  Int @default(autoincrement()) @id
-  name ProficiencyLevel
+  id      Int @default(autoincrement()) @id
+  name    ProficiencyLevel
   @@index([name])
 }
@@ -63,9 +67,9 @@
   EXPERT
 }
 model Language {
-  id  Int @default(autoincrement()) @id
-  code String
+  id      Int @default(autoincrement()) @id
+  code    String
   @@index([code])
 }
```


